fastapi==0.115.6
uvicorn[standard]==0.34.0
sqlalchemy==2.0.36
alembic==1.14.0
pydantic==2.10.4
pydantic-settings==2.7.1
PyJWT==2.10.1
python-multipart==0.0.20
qrcode[pil]==8.0
jinja2==3.1.5
psycopg2-binary==2.9.10
python-dotenv==1.0.1
# Only for the daily personal report's Google Doc publish (services/report_doc.py). It mints an
# ADC token for the runtime service account; the HTTP call itself stays on urllib, so this service
# still needs no `requests` and no google-api-python-client. Imported lazily, so an image without
# it boots normally and only that one feature reports itself unavailable.
google-auth==2.38.0
